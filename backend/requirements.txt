fastapi==0.111.0
uvicorn[standard]==0.29.0
# ultralytics 8.4.80 = MESMA versão que treinou/salvou o .pt. No 8.3.0 o modelo
# carregava mas o predict dava 500 (incompatibilidade de versão). torch fica sem
# pin: o ultralytics 8.4.80 puxa uma versão compatível e já allowlista os globals
# do checkpoint (o weights_only do torch>=2.6 não quebra mais).
ultralytics==8.4.80
supabase==2.4.2
huggingface_hub==0.23.4
python-multipart==0.0.9
Pillow==10.3.0
python-dotenv==1.0.1
numpy==1.26.4
opencv-python-headless==4.9.0.80
