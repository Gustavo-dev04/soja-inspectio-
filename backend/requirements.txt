fastapi==0.111.0
uvicorn[standard]==0.29.0
# torch fixado <2.6: evita o weights_only=True do torch>=2.6 na carga do .pt.
torch==2.2.2
torchvision==0.17.2
# ultralytics>=8.3.0: o modelo eh um YOLO11 (bloco C3k2), que so existe a
# partir do 8.3.0 — o 8.2.0 quebrava com "Can't get attribute 'C3k2'".
ultralytics==8.3.0
supabase==2.4.2
huggingface_hub==0.23.4
python-multipart==0.0.9
Pillow==10.3.0
python-dotenv==1.0.1
numpy==1.26.4
opencv-python-headless==4.9.0.80
