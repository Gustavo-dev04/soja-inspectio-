# Vígil.ia no Steam Deck (local).
#
# ⚠️ INSTALE O TORCH CPU ANTES deste requirements (o Deck é AMD; a build padrão
# do torch é CUDA, ~2 GB, e NÃO roda na GPU AMD — CPU é o caminho certo e menor):
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
ultralytics==8.4.80
opencv-python>=4.8.0
